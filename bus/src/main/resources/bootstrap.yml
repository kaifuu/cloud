spring:
  cloud:
    config:
      name: test
      profile: dev
      uri: http://localhost:8400/
      label: master
    bus:
      enabled: true
      trace:
        enabled: true #开启消息跟踪
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8000/eureka/


