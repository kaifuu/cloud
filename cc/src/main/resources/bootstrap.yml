spring:
  cloud:
    config:
      name: test
      profile: dev
      uri: http://localhost:8400/
      label: master
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8000/eureka/